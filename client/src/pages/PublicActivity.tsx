import CivicHeader, { CivicFooter } from "@/components/CivicHeader";
import { ActivityMap, type MapPin as ActivityPin } from "@/components/CivicMap";
import StatusPill from "@/components/StatusPill";
import { trpc } from "@/lib/trpc";
import { Map, MapPin as MapPinIcon, Rss, Sparkles } from "lucide-react";
import { Link } from "wouter";

export default function PublicActivity() {
  const activity = trpc.civicItems.publicList.useQuery({ limit: 24 });
  const map = trpc.civicItems.mapActivity.useQuery();
  const pins: ActivityPin[] = (map.data ?? []).map(item => ({ id: item.publicId, latitude: Number(item.latitude), longitude: Number(item.longitude), title: item.title, tone: item.status === "resolved" ? "teal" : item.priority === "urgent" ? "amber" : "blue" }));
  return <div className="min-h-screen bg-[#fbfdff]"><CivicHeader /><main className="container py-11 sm:py-14"><div className="grid gap-8 lg:grid-cols-[.78fr_1.22fr] lg:items-end"><div><p className="eyebrow">Community activity</p><h1 className="mt-3 text-3xl font-black tracking-[-.05em] text-[#153359] sm:text-4xl">See the civic record take shape.</h1><p className="mt-4 text-base leading-7 text-[#617994]">Public items are shown without reporter identities. The map is a starting point for local awareness, not a substitute for verified on-site assessment.</p><div className="mt-6 flex gap-4 text-xs font-bold text-[#58718d]"><span className="inline-flex items-center gap-1.5"><span className="h-2 w-2 rounded-full bg-[#0e5bb7]" />Open activity</span><span className="inline-flex items-center gap-1.5"><span className="h-2 w-2 rounded-full bg-[#0d9388]" />Resolved</span></div></div><div className="rounded-2xl border border-[#dce9f3] bg-[#f5fbff] p-5"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white text-[#0e5bb7] shadow-sm"><Map className="h-5 w-5" /></span><p className="text-sm leading-6 text-[#5a728d]">Location pins are added only when a citizen shares a map point. Private requests and personal account information are never shown here.</p></div></div></div><section className="mt-10"><div className="mb-4 flex items-center justify-between"><div className="flex items-center gap-2"><MapPinIcon className="h-5 w-5 text-[#0d9388]" /><h2 className="text-lg font-black text-[#23486f]">Activity by area</h2></div><span className="rounded-full bg-[#edf5ff] px-3 py-1 text-xs font-bold text-[#0e5bb7]">Google Maps</span></div><ActivityMap pins={pins} className="h-[430px]" /></section><section className="mt-12"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Latest public updates</p><h2 className="mt-2 text-2xl font-black tracking-[-.04em] text-[#17365d]">A shared view of local work.</h2></div><Rss className="hidden h-6 w-6 text-[#80a0bd] sm:block" /></div>{activity.isLoading ? <div className="mt-6 grid gap-4 md:grid-cols-2"><div className="h-44 animate-pulse rounded-2xl bg-[#eef5fa]" /><div className="h-44 animate-pulse rounded-2xl bg-[#eef5fa]" /></div> : activity.data?.length ? <div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{activity.data.map(item => <Link key={item.publicId} href={`/track?ref=${item.publicId}`} className="group rounded-2xl border border-[#dce8f3] bg-white p-5 shadow-[0_8px_22px_rgba(22,67,108,.04)] transition-all hover:-translate-y-1 hover:shadow-[0_14px_30px_rgba(22,67,108,.10)]"><div className="flex items-center justify-between gap-2"><p className="text-[11px] font-black uppercase tracking-[.13em] text-[#0e5bb7]">{item.publicId}</p><StatusPill status={item.status} /></div><h3 className="mt-3 line-clamp-2 text-base font-black text-[#23486f]">{item.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#6b829b]">{item.description}</p><div className="mt-4 border-t border-[#edf3f8] pt-3 text-xs font-bold text-[#7087a0]"><span className="inline-flex items-center gap-1"><MapPinIcon className="h-3.5 w-3.5" />{item.locationLabel}</span></div></Link>)}</div> : <div className="mt-6 rounded-[24px] border border-dashed border-[#cbdced] bg-white px-6 py-12 text-center"><span className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-[#eef6ff] text-[#0e5bb7]"><Sparkles className="h-5 w-5" /></span><h3 className="mt-4 text-lg font-black text-[#365674]">The public activity record is ready.</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[#7890ad]">There are no public items yet. Citizens can start contributing a privacy-conscious local request whenever they are ready.</p><Link href="/report" className="mt-5 inline-flex h-10 items-center rounded-lg bg-[#0e5bb7] px-4 text-sm font-extrabold text-white">Report an issue</Link></div>}</section></main><CivicFooter /></div>;
}
