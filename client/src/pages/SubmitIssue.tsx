import { useAuth } from "@/_core/hooks/useAuth";
import CivicHeader, { CivicFooter } from "@/components/CivicHeader";
import EvidencePicker from "@/components/EvidencePicker";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Textarea } from "@/components/ui/textarea";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, CircleAlert, FileAudio, FileText, LocateFixed, MapPin, Navigation, RotateCcw, ShieldCheck } from "lucide-react";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { Link } from "wouter";

const categories = [["roads", "Roads"], ["water", "Water"], ["sanitation", "Cleanliness"], ["electricity", "Lighting"], ["safety", "Safety"], ["environment", "Environment"], ["other", "Other"]] as const;
const DRAFT_KEY = "jananiti.report.draft.v1";
type Category = (typeof categories)[number][0];
type LocationMode = "auto" | "manual";
type UploadState = "idle" | "uploading" | "failed";
type ReportDraft = { title: string; description: string; category: Category; locationLabel: string; locationMode: LocationMode; latitude: string; longitude: string; visibility: "public" | "private" };

function mediaLabel(file: File | null) { if (!file) return ""; if (file.type.startsWith("video/")) return "Short video"; if (file.type.startsWith("audio/")) return "Voice note"; if (file.type.startsWith("image/")) return "Photo"; return "Document"; }
function contentType(file: File | null): "text" | "voice" | "image" | "mixed" { if (!file) return "text"; if (file.type.startsWith("audio/")) return "voice"; if (file.type.startsWith("image/")) return "image"; return "mixed"; }

export default function SubmitIssue() {
  const { user, loading } = useAuth();
  const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [category, setCategory] = useState<Category>("roads");
  const [locationLabel, setLocationLabel] = useState(""); const [locationMode, setLocationMode] = useState<LocationMode>("auto"); const [point, setPoint] = useState<{ latitude: number; longitude: number } | null>(null);
  const [latitude, setLatitude] = useState(""); const [longitude, setLongitude] = useState(""); const [visibility, setVisibility] = useState<"public" | "private">("public");
  const [attachment, setAttachment] = useState<File | null>(null); const [preview, setPreview] = useState<string | null>(null); const [locationError, setLocationError] = useState(""); const [uploadError, setUploadError] = useState("");
  const [uploadState, setUploadState] = useState<UploadState>("idle"); const [uploadProgress, setUploadProgress] = useState(0); const [createdPublicId, setCreatedPublicId] = useState<string | null>(null); const [submitted, setSubmitted] = useState<string | null>(null);
  const [hydrated, setHydrated] = useState(false); const [hasSavedDraft, setHasSavedDraft] = useState(false); const [reviewing, setReviewing] = useState(false);
  const create = trpc.civicItems.create.useMutation();
  const saveProfile = trpc.profile.save.useMutation();
  const attachmentLabel = useMemo(() => mediaLabel(attachment), [attachment]);
  const categoryLabel = categories.find(([value]) => value === category)?.[1] ?? category;

  useEffect(() => { try { const raw = window.localStorage.getItem(DRAFT_KEY); if (raw) { const draft = JSON.parse(raw) as Partial<ReportDraft>; setTitle(draft.title ?? ""); setDescription(draft.description ?? ""); setCategory(draft.category ?? "roads"); setLocationLabel(draft.locationLabel ?? ""); setLocationMode(draft.locationMode ?? "auto"); setLatitude(draft.latitude ?? ""); setLongitude(draft.longitude ?? ""); setVisibility(draft.visibility ?? "public"); setHasSavedDraft(Boolean(draft.title || draft.description || draft.locationLabel)); } } catch { window.localStorage.removeItem(DRAFT_KEY); } finally { setHydrated(true); } }, []);
  useEffect(() => { if (!hydrated || submitted) return; const draft: ReportDraft = { title, description, category, locationLabel, locationMode, latitude, longitude, visibility }; if (title || description || locationLabel || latitude || longitude) { window.localStorage.setItem(DRAFT_KEY, JSON.stringify(draft)); setHasSavedDraft(true); } }, [category, description, hydrated, latitude, locationLabel, locationMode, longitude, submitted, title, visibility]);
  useEffect(() => { if (!attachment || !/^(image|audio|video)\//.test(attachment.type)) { setPreview(null); return; } const url = URL.createObjectURL(attachment); setPreview(url); return () => URL.revokeObjectURL(url); }, [attachment]);

  const clearDraft = () => { window.localStorage.removeItem(DRAFT_KEY); setHasSavedDraft(false); };
  const chooseAttachment = (file: File | null) => { if (file && file.size > 10 * 1024 * 1024) { setAttachment(null); setUploadError("Choose a file no larger than 10 MB."); return; } setAttachment(file); setUploadError(""); setUploadState("idle"); };
  const useCurrentLocation = () => { setLocationError(""); if (!navigator.geolocation) return setLocationError("This browser cannot provide automatic location. Use manual location instead."); navigator.geolocation.getCurrentPosition(pos => { const next = { latitude: pos.coords.latitude, longitude: pos.coords.longitude }; setPoint(next); setLatitude(next.latitude.toFixed(6)); setLongitude(next.longitude.toFixed(6)); setLocationLabel(`Current location · ${next.latitude.toFixed(5)}, ${next.longitude.toFixed(5)}`); }, () => setLocationError("Location permission was not granted. You can enter a landmark or coordinates manually."), { enableHighAccuracy: false, timeout: 10000 }); };
  const uploadEvidence = (publicId: string, file: File) => new Promise<void>((resolve, reject) => { setUploadState("uploading"); setUploadProgress(0); const request = new XMLHttpRequest(); request.open("POST", `/api/civic-items/${publicId}/attachments`); request.setRequestHeader("Content-Type", file.type || "application/octet-stream"); request.setRequestHeader("x-file-name", encodeURIComponent(file.name)); request.upload.onprogress = event => { if (event.lengthComputable) setUploadProgress(Math.round(event.loaded / event.total * 100)); }; request.onerror = () => reject(new Error("Evidence upload could not be completed. Check your connection and retry.")); request.onload = () => { if (request.status >= 200 && request.status < 300) resolve(); else reject(new Error("Evidence upload could not be completed.")); }; request.send(file); });
  const finishUpload = async (publicId: string, file: File) => { try { await uploadEvidence(publicId, file); setUploadState("idle"); setUploadProgress(100); clearDraft(); setSubmitted(publicId); } catch (error) { setUploadState("failed"); setUploadError(error instanceof Error ? error.message : "Evidence upload could not be completed."); } };
  const openReview = (event: FormEvent) => { event.preventDefault(); setUploadError(""); if (!user) return startLogin(); const lat = point?.latitude ?? (latitude ? Number(latitude) : null); const lng = point?.longitude ?? (longitude ? Number(longitude) : null); if (!locationLabel.trim()) return setUploadError("Add a locality, landmark, or automatic location before continuing."); if ((lat !== null && !Number.isFinite(lat)) || (lng !== null && !Number.isFinite(lng))) return setUploadError("Use valid decimal coordinates."); setReviewing(true); };
  const publish = async () => { setUploadError(""); const lat = point?.latitude ?? (latitude ? Number(latitude) : null); const lng = point?.longitude ?? (longitude ? Number(longitude) : null); try { if (visibility === "public" && locationLabel.trim()) await saveProfile.mutateAsync({ locality: locationLabel.trim() }); const result = await create.mutateAsync({ title, description, category, locationLabel, visibility, latitude: lat, longitude: lng, contentType: contentType(attachment) }); setCreatedPublicId(result.publicId); if (attachment) await finishUpload(result.publicId, attachment); else { clearDraft(); setSubmitted(result.publicId); } } catch (error) { setUploadError(error instanceof Error ? error.message : "The civic update could not be published."); } };

  if (submitted) return <div className="min-h-screen bg-[#f6f8fb]"><CivicHeader /><main className="container py-20"><div className="mx-auto max-w-lg rounded-[32px] bg-white p-9 text-center shadow-[0_20px_60px_rgba(22,59,97,.12)]"><CheckCircle2 className="mx-auto h-14 w-14 text-[#0d9388]" /><p className="mt-5 text-xs font-black uppercase tracking-[.16em] text-[#0d9388]">Published to the civic record</p><h1 className="mt-2 text-3xl font-black tracking-[-.05em] text-[#17365d]">Your update has a clear path forward.</h1><p className="mt-4 text-sm leading-6 text-[#6b829b]">Your identity remains private. Follow the progress and community context from your personal record.</p><p className="mt-6 rounded-2xl bg-[#eef6ff] py-3 font-black tracking-[.12em] text-[#0e5bb7]">{submitted}</p><div className="mt-7 grid gap-3 sm:grid-cols-2"><Link href={`/track?ref=${submitted}`} className="rounded-xl bg-[#0e5bb7] px-4 py-3 text-sm font-extrabold text-white">Track update</Link><Link href="/activity" className="rounded-xl border border-[#cbdced] px-4 py-3 text-sm font-extrabold text-[#315474]">See local feed</Link></div></div></main><CivicFooter /></div>;
  if (reviewing) return <div className="min-h-screen bg-[#f6f8fb]"><CivicHeader /><main className="container py-8 sm:py-12"><div className="mx-auto max-w-2xl"><button type="button" onClick={() => setReviewing(false)} className="text-sm font-extrabold text-[#0e5bb7]">← Edit report</button><section className="mt-5 rounded-[30px] bg-white p-6 shadow-[0_14px_38px_rgba(31,75,116,.08)] sm:p-8"><p className="eyebrow">Review and confirm</p><h1 className="mt-2 text-3xl font-black tracking-[-.055em] text-[#17365d]">Check the civic record before it is published.</h1><p className="mt-3 text-sm leading-6 text-[#68809a]">You remain in control. Every field can be changed before your report is created.</p><div className="mt-6 rounded-2xl border border-[#d3e8e4] bg-[#effaf8] p-4"><p className="text-xs font-black uppercase tracking-[.14em] text-[#087b72]">Development suggestion</p><p className="mt-2 text-sm font-black text-[#315474]">This structured summary uses your selected category and text only.</p><p className="mt-2 text-xs leading-5 text-[#5d827c]">Vertex AI/Gemini is not configured in this environment. This is a deterministic development preview, not an automated civic decision.</p></div><div className="mt-5 grid gap-px overflow-hidden rounded-2xl border border-[#e0eaf2] bg-[#e0eaf2] sm:grid-cols-2"><div className="bg-white p-4"><p className="text-xs font-bold text-[#7890ad]">Category</p><p className="mt-1 text-sm font-black text-[#315474]">{categoryLabel}</p></div><div className="bg-white p-4"><p className="text-xs font-bold text-[#7890ad]">Location</p><p className="mt-1 text-sm font-black text-[#315474]">{locationLabel}</p></div><div className="bg-white p-4 sm:col-span-2"><p className="text-xs font-bold text-[#7890ad]">Issue title</p><p className="mt-1 text-sm font-black text-[#315474]">{title}</p></div><div className="bg-white p-4 sm:col-span-2"><p className="text-xs font-bold text-[#7890ad]">What people will see</p><p className="mt-1 text-sm leading-6 text-[#526f89]">{description}</p></div>{attachment && <div className="bg-white p-4 sm:col-span-2"><p className="text-xs font-bold text-[#7890ad]">Evidence</p><p className="mt-1 text-sm font-black text-[#315474]">{attachmentLabel}: {attachment.name}</p></div>}</div>{uploadState === "failed" && createdPublicId && attachment && <div className="mt-5 flex flex-wrap items-center justify-between gap-3 rounded-2xl border border-[#f0d1cb] bg-[#fff7f6] p-4"><p className="text-sm font-bold text-[#9e3c2c]">The record <b>{createdPublicId}</b> exists, but the evidence upload needs another try.</p><button type="button" onClick={() => finishUpload(createdPublicId, attachment)} className="rounded-lg bg-[#a84936] px-3 py-2 text-xs font-extrabold text-white">Retry evidence upload</button></div>}{uploadError && <p className="mt-5 flex items-center gap-2 text-sm font-bold text-[#b1412d]"><CircleAlert className="h-4 w-4" />{uploadError}</p>}<div className="mt-7 grid gap-3 sm:grid-cols-2"><button type="button" onClick={() => setReviewing(false)} className="rounded-xl border border-[#cbdced] px-4 py-3 text-sm font-extrabold text-[#315474]">Edit report</button><button type="button" disabled={create.isPending || uploadState === "uploading" || uploadState === "failed"} onClick={publish} className="rounded-xl bg-[#0e5bb7] px-4 py-3 text-sm font-extrabold text-white disabled:opacity-60">{create.isPending ? "Creating record…" : "Confirm and publish"}</button></div></section></div></main><CivicFooter /></div>;

  return <div className="min-h-screen bg-[#f7f8fa]"><CivicHeader /><main className="container py-6 sm:py-10"><div className="mx-auto max-w-3xl"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#0064e0]">Create a civic report</p><h1 className="mt-2 text-[30px] font-semibold leading-[1.12] tracking-[-.04em] text-[#0a1317] sm:text-4xl">What needs attention near you?</h1><p className="mt-3 max-w-xl text-sm leading-6 text-[#5d6c7b]">Start with the essential detail. Add a clear photo, voice note, video, or document only when it helps people act.</p></div><Link href="/activity" className="inline-flex min-h-11 items-center text-sm font-bold text-[#0064e0]">Back to local feed</Link></div>{!loading && !user && <button type="button" onClick={startLogin} className="mt-5 flex w-full items-center gap-3 rounded-[20px] border border-[#dfe5eb] bg-white p-4 text-left text-sm text-[#44515e] transition hover:border-[#0064e0]"><ShieldCheck className="h-5 w-5 shrink-0 text-[#31a24c]" /><span><b className="text-[#0a1317]">Sign in to publish.</b> Your post stays linked to a private status record.</span></button>}{hasSavedDraft && <div className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-[20px] border border-[#d6eadc] bg-[#f2fbf4] px-4 py-3 text-sm text-[#315f43]"><span className="flex items-center gap-2"><RotateCcw className="h-4 w-4 text-[#31a24c]" /><b>Local draft saved.</b> Evidence files are never stored in the draft.</span><button type="button" onClick={clearDraft} className="text-xs font-bold text-[#087b72] hover:underline">Clear saved copy</button></div>}<form onSubmit={openReview} className="mt-6 space-y-4"><section className="rounded-[28px] border border-[#e1e6eb] bg-white p-4 shadow-[0_1px_4px_rgba(20,22,26,.08)] sm:p-7"><div className="flex flex-wrap gap-2">{categories.map(([value, label]) => <button type="button" key={value} onClick={() => setCategory(value)} className={`rounded-full px-3.5 py-2 text-xs font-bold transition active:scale-[.98] motion-reduce:transform-none ${category === value ? "bg-[#0a1317] text-white" : "border border-[#dfe5eb] bg-white text-[#444950] hover:border-[#0064e0]"}`}>{label}</button>)}</div><Input required minLength={8} maxLength={160} value={title} onChange={event => setTitle(event.target.value)} placeholder="Give this issue a simple title" className="mt-5 h-12 rounded-lg border-[#ced0d4] px-3 text-base font-semibold shadow-none focus-visible:ring-[#0064e0]" /><Textarea required minLength={20} maxLength={3000} value={description} onChange={event => setDescription(event.target.value)} placeholder="What is happening? Mention impact, timing, and a helpful landmark." className="mt-3 min-h-28 resize-none rounded-lg border-[#ced0d4] px-3 py-3 text-sm leading-6 shadow-none focus-visible:ring-[#0064e0]" /><div className="mt-4"><EvidencePicker file={attachment} onChoose={chooseAttachment} onRemove={() => chooseAttachment(null)} /></div>{attachment && <div className="mt-3 overflow-hidden rounded-[20px] border border-[#e0e9f2] bg-[#f8fbfe] p-3">{attachment.type.startsWith("image/") && preview && <img src={preview} alt="Selected evidence preview" className="max-h-64 w-full rounded-xl object-cover" />}{attachment.type.startsWith("video/") && preview && <video src={preview} controls className="max-h-64 w-full rounded-xl bg-slate-950" />}{attachment.type.startsWith("audio/") && preview && <div className="flex items-center gap-3 rounded-xl bg-white p-3"><FileAudio className="h-5 w-5 text-[#0064e0]" /><audio controls src={preview} className="h-8 min-w-0 flex-1" /></div>}{!preview && <div className="flex items-center gap-2 rounded-xl bg-white p-3 text-xs text-[#687f96]"><FileText className="h-4 w-4 text-[#0064e0]" />Document evidence will be attached after publishing.</div>}</div>}</section><section className="rounded-[28px] border border-[#e1e6eb] bg-white p-4 shadow-[0_1px_4px_rgba(20,22,26,.08)] sm:p-7"><div className="flex items-center justify-between"><div><p className="text-sm font-bold text-[#0a1317]">Where is this happening?</p><p className="mt-1 text-xs text-[#5d6c7b]">Choose automatic location or enter it yourself. You can correct the label.</p></div><MapPin className="h-5 w-5 text-[#0064e0]" /></div><div className="mt-4 grid grid-cols-2 gap-2"><button type="button" onClick={() => setLocationMode("auto")} className={`rounded-[18px] border p-3 text-left transition active:scale-[.98] motion-reduce:transform-none ${locationMode === "auto" ? "border-[#0064e0] bg-[#f4f8ff]" : "border-[#dfe5eb]"}`}><LocateFixed className="h-4 w-4 text-[#0064e0]" /><b className="mt-2 block text-sm text-[#0a1317]">Use my location</b><span className="mt-1 block text-xs text-[#5d6c7b]">You control permission</span></button><button type="button" onClick={() => setLocationMode("manual")} className={`rounded-[18px] border p-3 text-left transition active:scale-[.98] motion-reduce:transform-none ${locationMode === "manual" ? "border-[#0064e0] bg-[#f4f8ff]" : "border-[#dfe5eb]"}`}><Navigation className="h-4 w-4 text-[#0064e0]" /><b className="mt-2 block text-sm text-[#0a1317]">Enter manually</b><span className="mt-1 block text-xs text-[#5d6c7b]">Landmark or coordinates</span></button></div>{locationMode === "auto" && <div className="mt-3 rounded-[18px] bg-[#f1f4f7] p-3"><Button type="button" variant="outline" onClick={useCurrentLocation} className="border-[#c8d9f1] text-[#0064e0]"><LocateFixed className="mr-2 h-4 w-4" />Capture current location</Button><p className="mt-2 text-xs leading-5 text-[#5d6c7b]">Only approximate coordinates are saved for routing. Your identity is never placed on a public post.</p></div>}<div className="mt-4 space-y-3"><Input value={locationLabel} onChange={event => setLocationLabel(event.target.value)} placeholder="Locality, street, or nearby landmark" className="h-11 rounded-lg border-[#ced0d4]" /><div className="grid grid-cols-2 gap-2"><Input type="number" step="any" value={latitude} onChange={event => { setLatitude(event.target.value); setPoint(null); }} placeholder="Latitude (optional)" /><Input type="number" step="any" value={longitude} onChange={event => { setLongitude(event.target.value); setPoint(null); }} placeholder="Longitude (optional)" /></div></div>{locationMode === "auto" && locationLabel && <p className="mt-3 text-xs font-bold text-[#087b72]">Location ready: {locationLabel}</p>}{locationError && <p className="mt-3 text-xs font-bold text-[#b1412d]">{locationError}</p>}</section><section className="flex flex-col gap-4 rounded-[28px] bg-[#0a1317] p-4 text-white sm:flex-row sm:items-center sm:justify-between sm:p-6"><div><p className="font-bold">Share with your neighbourhood?</p><p className="mt-1 text-xs leading-5 text-[#d5dbe0]">Your identity stays private. Public updates show a generalized location.</p><button type="button" onClick={() => setVisibility(visibility === "public" ? "private" : "public")} className="mt-3 rounded-full bg-white/10 px-3 py-1.5 text-xs font-bold">{visibility === "public" ? "Public community update" : "Private request"}</button></div><Button type="submit" disabled={create.isPending || uploadState === "uploading"} className="h-12 rounded-full bg-[#0064e0] px-6 font-bold text-white hover:bg-[#0457cb]">{user ? "Review civic update" : "Sign in to publish"}</Button></section>{uploadError && <p className="flex items-center gap-2 text-sm font-bold text-[#b1412d]"><CircleAlert className="h-4 w-4" />{uploadError}</p>}</form></div></main><CivicFooter /></div>;
}
